# Holdout Status

No untouched holdout dataset exists for the promoted r6 experiment. The
60-cell expanded matrix was evaluated after controller selection and is kept
under `data/validation`, not relabeled as holdout evidence.

A future holdout must be generated from previously unseen fault depths,
durations, inception angles, and plant parameters before the proxy or actor is
retuned against those cases.
