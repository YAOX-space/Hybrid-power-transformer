# Collectors

Switch-level data collection scripts.  These produce FRT calibration matrices,
trajectory traces, and teacher traces under `lab/results/`.

Run them from `version_2/simulink` with `run(fullfile(pwd,'collectors','<script>.m'))`.

